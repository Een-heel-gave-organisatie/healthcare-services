{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "title": "healthcare_immunization_registry_api GET_health Operation Response Schema",
    "description": "Schema representing the output of the GET_health operation in healthcare_immunization_registry_api",
    "properties": {
        "payload": {
  "type" : "object",
  "properties" : {
    "status" : {
      "type" : "string",
      "example" : "OK"
    },
    "timestamp" : {
      "type" : "string",
      "format" : "date-time",
      "example" : "2024-01-15T10:30:00Z"
    }
  }
},
      "attributes": {
        "type": "object",
        "description": "Metadata about the operation.",
        "properties": {
          "statusCode": {
            "type": "integer",
            "description": "HTTP status code of the API response.",
            "minimum": 100,
            "maximum": 599
          }
        },
        "required": ["statusCode"],
        "additionalProperties": false
      },
      "headers": {
        "type": "object",
        "description": "HTTP headers of the API response.",
        "additionalProperties": false
      }
    },
    "required": ["payload", "attributes", "headers"],
    "additionalProperties": false
  }
